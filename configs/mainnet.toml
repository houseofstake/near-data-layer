# MAINNET Configuration for NEAR Indexer
# This configuration file provides production values for the NEAR indexer running on mainnet.
# Values marked with "set_in_env" and any other settings can be overridden
# by environment variables using the format "INDEXER_{SECTION}_{KEY}"
# For example: INDEXER_DB_HOST=localhost will override db_host

# Database Configuration - set via environment variables
# db_host, db_database, db_username, db_password set via INDEXER_* env vars
db_database = "postgres"
db_port = 5432
db_schema = "fastnear"
db_max_connections = 32

# Indexer Configuration
start_block = 154850000 # first block with voteagora contract action 158,487,483 https://nearblocks.io/txns/EDxgoJDkZjEYdjSMMgvkq1x1QfQ7EibnAxbF9WXdP1ri?tab=execution#4MJfN2K3S1saXimYKsYzjWKERZ7pgZArm1yrySV9Cu1Q
poll_interval = 30
retry_delay = 1
num_threads = 32

# Other
log_level = "info"
app_version = "v3.0.1"

# DataDog Configuration - set via environment variables
# dd_api_key, environment, dd_environment set via INDEXER_* env vars  
datadog_enabled = true

# Contract Configuration
hos_contracts = [
    'voteagora.near'
] 
